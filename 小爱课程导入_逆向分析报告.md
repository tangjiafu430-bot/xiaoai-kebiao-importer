# 「小爱课程导入」APK 逆向分析报告

> 分析对象：`小爱课程导入_2026030302(2026030302).apk`（2.3 MB）
> 分析时间：2026-08-30

## 一、应用概览

| 项目 | 内容 |
|---|---|
| 应用名 | 小爱课程导入 |
| 包名 | `com.mercury.courseimport` |
| 版本 | 2026030302 |
| 作者来源 | 酷安用户（www.coolapk.com/u/3336736），更新托管在蓝奏云 wwaoh.lanzout.com/b0w9kyeaf |
| 技术栈 | Kotlin + OkHttp + Gson/org.json + 协程，minSdk 26 / targetSdk 34 |
| 权限 | 仅 `INTERNET`（无任何敏感权限） |
| 组件 | `MainActivity`（主界面）、`BrowserActivity`（内嵌 WebView 登录教务系统） |

## 二、应用工作流程

1. **获取凭据**：用户在「Debug JSON」输入框粘贴一段 JSON（来自小爱 App 的课表 H5 页面调试信息），其中包含 `appId`、`authorization`（或 `serviceToken`）、`deviceId`。
2. **抓取课表源数据**：`BrowserActivity` 用 WebView 打开学校教务系统网址，用户登录后，页面 HTML 被保存到缓存文件 `captured_page.html` 回传给主界面。
3. **AI 解析**：将 HTML 连同系统提示词发给阿里云 DashScope（OpenAI 兼容模式，默认模型 `qwen3-coder-plus`，SSE 流式），模型返回结构化 JSON（courses + schedule）。
4. **本地校验/纠正**：`Course.sanitizeAndValidate()` 校验每门课，自动展开「1-16周(单)」这类单双周和节次区间。
5. **写入小爱课表**：调用 `i.ai.mi.com` 的 `course-multi-auth` 系列接口——查询课表列表 →（可选）新建课表/删除旧课 → 批量写入课程 → 同步课表设置（节数、上下课时间等） →（可选）切换当前课表。

## 三、鉴权机制（核心）

`MainActivity.w()` 从 Debug JSON 中提取三个字段：
- `appId`（数字字符串）；若 JSON 里只有 `authorization` 字段且含 `app_id:(\d+)`，则用正则提取
- `serviceToken`（从 `authorization` 或 `serviceToken` 字段取）
- `deviceId`

`g2.a.a(appId, serviceToken, deviceId)` 构造 `Authorization` 请求头：

```
若 serviceToken 以 "DO-TOKEN" 或 "AO-TOKEN" 开头 → 直接原样使用

否则 →
AO-TOKEN-V1 dev_app_id:{appId},access_token:{serviceToken},scope_data:{base64}
其中 base64 = Base64.NO_WRAP( UTF8( {"d":"{deviceId}"} ) )
```

## 四、API 接口文档（逆向还原）

所有接口公共请求头：

| Header | 值 |
|---|---|
| Authorization | 见上文鉴权 |
| Content-Type | application/json |
| X-Requested-With | `com.xiaomi.aischedule`（table_switch 用 `com.miui.voiceassist`） |
| Referer | `https://i.ai.mi.com/h5/precache/ai-schedule/` |
| User-Agent | `Mozilla/5.0 (Linux; Android 16; wv) AppleWebKit/537.36 Mobile Safari/537.36 AgentWeb/4.1.3` |

响应统一为 `{"code":0, "data":..., "desc":...}`；HTTP 401/500 视为认证失效。

### 1. 查询课表列表
```
GET https://i.ai.mi.com/course-multi-auth/tables?requestId={32位大写UUID无横线}&sourceName=course-app-aiSchedule
```
返回 `data` 为数组：`[{id, name, current, setting{...}}]`，`current==1` 为当前使用课表。

### 2. 查询课表详情（含课程）
```
GET https://i.ai.mi.com/course-multi-auth/table?ctId={ctId}&requestId={UUID}&sourceName=course-app-aiSchedule
```
返回 `data` 内含 `courses`、`setting`。

### 3. 新建课表
```
POST https://i.ai.mi.com/course-multi-auth/table
{"name":"新课表名", "current":0, "sourceName":"course-app-aiSchedule"}
```
返回 `data` 为新 ctId（long）。

### 4. 同步课表设置（PUT 更新）
```
PUT https://i.ai.mi.com/course-multi-auth/table
{
  "ctId": 123,
  "name": "课表名",
  "sourceName": "course-app-aiSchedule",
  "setting": {
    "presentWeek": 1, "totalWeek": 20, "startSemester": "...", "id": ...,
    "isWeekend": 0, "morningNum": 5, "afternoonNum": 4, "nightNum": 3,
    "speak": ..., "weekStart": ...,
    "sections": "[{\"i\":1,\"s\":\"08:00\",\"e\":\"08:45\"},...]",  // 转义的JSON字符串
    "sectionTimes": ..., "school": "{}",
    "extend": "{\"bgSetting\":..., \"degree\":..., \"showNotInWeek\":...}"
  }
}
```

### 5. 批量写入课程（核心导入接口）
```
POST https://i.ai.mi.com/course-multi-auth/courseInfos
额外头：RequestId: {32位大写UUID无横线}、Origin: https://i.ai.mi.com
{
  "ctId": 123,
  "sourceName": "course-app-aiSchedule",
  "courses": [
    {
      "name": "高等数学",
      "position": "教1-101",
      "teacher": "张三",
      "day": 1,                       // 1=周一 … 7=周日
      "sections": "1,2",              // 逗号分隔节次
      "weeks": "1,2,3,...,16",        // 逗号分隔周次
      "style": "{\"color\":\"#00A6F2\",\"background\":\"#E5F4FF\"}"  // 转义JSON
    }
  ]
}
```
同一门课名循环使用 12 种预设颜色（`#00A6F2/#FC6B50/#3CB3C8/#7D7AEA/#FF9900/#EF5B75/#5B8EFF/#F067BB/#29BBAA/#CBA713/#B967E3/#6E8ADA`，各有对应浅色背景）。返回 `status != -1` 且 `code` 为 0/200 即成功。

### 6. 删除课程
```
DELETE https://i.ai.mi.com/course-multi-auth/courseInfo
{"ctId": 123, "cId": 456, "sourceName":"course-app-aiSchedule"}
```

### 7. 切换当前课表（小爱同学域名）
```
POST https://i.xiaomixiaoai.com/course-multi-auth/table_switch
额外头：X-Requested-With: com.miui.voiceassist、Origin: https://i.xiaomixiaoai.com、
        Referer: https://i.xiaomixiaoai.com/h5/precache/ai-schedule/、
        User-Agent: Mozilla/5.0 (Linux; Android 16; wv) AppleWebKit/537.36 Mobile Safari/537.36
{"fromCtId": 111, "toCtId": 222, "sourceName":"course-app-miui"}
```

## 五、AI 解析环节

- 端点：用户可配置，默认 `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`，`Authorization: Bearer {用户API Key}`，`stream: true`（SSE 解析 `data: {...}` 直到 `data: [DONE]`，拼接 `delta.content`，兼容 `reasoning_content`）。
- 默认模型：`qwen3-coder-plus`，temperature 由界面传入。
- 系统提示词（原文）要求模型从教务系统 HTML 中提取：

```json
{
  "courses": [
    {"name":"课程名称","teacher":"教师姓名","position":"上课地点","day":1,"sections":"1,2","weeks":"1,2,...,16"}
  ],
  "schedule": {
    "morningNum": 5, "afternoonNum": 4, "nightNum": 3,
    "sections": "[{\"i\":1,\"s\":\"08:00\",\"e\":\"08:45\"},...]"
  }
}
```
规则要点：同一课程不同周次/时间必须拆成独立对象；schedule 各字段独立可选、严禁编造；输出纯 JSON 不得带 ```json 包裹。
- 客户端还内置 JSON 自动修复（识别「键值对缺少冒号 / 括号不闭合 / 字符串未闭合 / JSON 被截断」等 org.json 异常并翻译成中文提示）。

## 六、本地课程校验与纠正规则（Course 类）

- 标准格式校验：`^\d+(,\d+)*$`（sections / weeks 必须是纯逗号分隔数字）。
- 区间展开正则：`(\d+)\s*(?:-|至|~)\s*(\d+)(?:[\s周\(（\)）]*(单|双)[\s周\(（\)）]*)?|(\d+)`
  - 支持 `1-16`、`1至16`、`1~16`，以及带 `(单)` / `(双)` 的单双周（只保留奇/偶数周）。
  - 自动展开后会标记 `isAutoCorrected`，UI 提示用户核对。
- 校验失败（缺课程名、day/sections/weeks 不合法）的课程标记 `isInvalid`，导入时跳过并在明细弹窗中标红列出。

## 七、给「课表脚本」项目的复刻建议

1. 用 Python `requests` 实现第四节 7 个接口即可完全复刻导入能力，`requests` 的 `params`/`json` 会自动处理编码。
2. Authorization 头构造见第三节；`requestId` 只需 `uuid.uuid4().hex.upper()`。
3. 推荐导入流程：查询列表 →（可选）新建课表 → 批量写课程 → PUT 同步 setting（节数/时间表） → 切换课表。
4. AI 解析可换成任意 OpenAI 兼容端点（含本地模型），提示词可直接照抄第五节。
5. Debug JSON 凭据来自小爱课表 H5 页面的用户调试信息，`serviceToken` 有有效期，401/500 时需重新抓取。
